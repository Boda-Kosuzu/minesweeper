### マインスイーパー(マス)の遊び方

ローカルにクローンして、`npm start`すると`localhost:5000`にアクセスすると遊べます。

### マス？

鱒です。
マス目の変数名をtroutにしたところ、これは鱒だとツッコミをいただいたので、そのまま鱒になりました。

### その他メモ

#### 利用しているライブラリ

* Svelt 
  * 正確にはコンパイラ。svelteファイルをjs, html, cssファイルに変換する。
* fa-svelt
  * svelteファイル内でFont awesomeを利用できるようにしてくれるライブラリ。